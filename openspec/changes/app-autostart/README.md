# app-autostart

Auto-launch a single favorite installed Xbox app when XBVault connects. Per-app flyout option in Installed tab (icon + confirm dialog), exclusive badge, launch via existing LaunchPackageAsync.
