# app-updates

Background job (while connected) detects catalog updates for installed Xbox apps; single consolidated notification listing all updatable apps, each clickable to open the existing update dialog; follow-up via notification center.
