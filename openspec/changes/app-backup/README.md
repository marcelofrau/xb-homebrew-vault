# app-backup

Backup installed Xbox apps to a ZIP on PC: appx if available + LocalAppData/LocalState (REST via PortalAppFilesService) + user-selected remote folders (SFTP, multi-select dialog like file explorer).
