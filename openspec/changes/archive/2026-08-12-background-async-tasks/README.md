# background-async-tasks

Foundation: background task service (one-shot tasks + recurring jobs), connection monitor, toast primitive, and IntelliJ-style task center (status bar indicator + in-window overlay panel).
